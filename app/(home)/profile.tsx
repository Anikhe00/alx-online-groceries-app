import { Text, View } from "react-native";

const Profile = () => {
  return (
    <View
      style={{
        flex: 1,
        width: "100%",
        height: "auto",
        alignItems: "center",
        justifyContent: "center",
      }}
    >
      <Text>Profile</Text>
    </View>
  );
};

export default Profile;
